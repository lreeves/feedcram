%table.table
  %tr
    %th Title
    %th Feed
    %th Date
  - @viewed.each do |viewed|
    %tr
      %td= link_to viewed.feed_entry.title, entry_path(viewed.feed_entry)
      %td= viewed.feed_entry.feed.title
      %td= viewed.created_at.blank? ? '' : viewed.created_at.to_date

= will_paginate @viewed
